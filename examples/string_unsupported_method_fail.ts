// Phase 1.5-6 prep #10: only `.charCodeAt` and `.slice` are supported on
// `topaz_string` for now. Other JS String methods are explicitly rejected.
const s: string = "hello";
console.log(s.indexOf("l"));
