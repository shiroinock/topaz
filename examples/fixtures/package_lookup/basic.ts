import { topazValue } from "topaz-pkg";
import { indexValue } from "index-pkg";
import { scopedValue } from "@scope/topaz-pkg";

console.log(topazValue());
console.log(indexValue());
console.log(scopedValue());
