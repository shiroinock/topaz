# TS AOT ネイティブコンパイラ 設計メモ

TypeScript 構文をフロントエンドにして、JS のセマンティクスを切り捨てた上で **真の AOT ネイティブコンパイル** を狙うプロジェクトの設計検討資料。Matz の Ruby AOT コンパイラ [spinel](https://github.com/matz/spinel) を直接の参考にする。

現状の仕様(設計上の固定点・既知の divergence・実装されたサンプル)は `CLAUDE.md` を参照。過去 Phase の「なぜそう実装したか」の決定ログは `docs/archive/implementation-log.md` を参照(新 Phase の決定ログもそこへ追記する運用)。

---

## 1. 背景 — 既存の「TS → バイナリ」アプローチの整理

### 1.1 ランタイム同梱型(実用解、ただし真の AOT ではない)

ランタイムごとバイナリにパッケージするアプローチ。実態は自己解凍 fat binary。

- **Bun** (`bun build --compile`): TS 直接入力可、バイナリ 50–100MB
- **Deno** (`deno compile`): 同上
- **Node.js SEA**: 公式、TS は事前 JS 化必要
- **pkg / nexe**: 古典、Node の古いバージョン依存

→ 配布 CLI なら現実的だが「ネイティブコンパイル」ではない。

### 1.2 真の AOT ネイティブコンパイル(研究・実験段階)

| 名前 | 戦略 | 状態 |
|---|---|---|
| **Static Hermes (shermes)** | Hermes を拡張、sound TS 型でランタイム検査を省く。C / LLVM 経由でネイティブ機械語に AOT | 実験的、TS サポート不完全、バイナリ配布なし |
| **Porffor** | ゼロから書かれた JS/TS → Wasm/C AOT。ランタイム同梱せず、バイナリ 1000x 小さい | Test262 通過率 ~35%、研究プロジェクト |
| **AssemblyScript** | TS 構文の別言語(strict、`i32`/`i64` あり、Wasm 専用) | 実用段階、Wasm 用途で実績あり |

このプロジェクトの位置づけは「AssemblyScript の哲学(TS 構文を借りた別言語)」+「Static Hermes/Porffor の AOT 技術」+「Spinel の現実的スコープ感」。

---

## 2. Hermes と Static Hermes の整理

### Hermes(2019, Meta)

React Native 向け JS エンジン。**JIT を持たず、ビルド時に Hermes Bytecode (HBC) に AOT コンパイル**。

- JIT を捨てる代わりに、起動・メモリ・バイナリサイズで勝つ設計
- iOS の JIT 禁止制約とも親和性が高い
- ランタイム本体が小さく、HBC は mmap 可能 → コールドスタート最適

### Static Hermes

Hermes の延長で、**型情報を信じて機械語まで AOT する**新世代:

1. TS/Flow の sound な型を使ってランタイム型検査を省略
2. C 経由 / LLVM 経由でネイティブバイナリに落とす
3. 動的部分は JIT で補う

「Static」は「事前コンパイル」ではなく「**型情報を静的に使う**」の意味。Hermes V1 として React Native のデフォルトに置き換わる流れ。

### モバイル以外での活用

Static Hermes 自体はモバイル特化ではないため、以下が候補:

- **CLI ツール配布**: Bun/Deno の compile 競合。バイナリ数百 KB 〜数 MB(対 50–100MB)
- **組み込みスクリプティング**: QuickJS / Lua 置き換え
- **エッジワーカーのコールドスタート最適化**: V8 isolate の ~5ms を 0ms 近くへ
- **シェルスクリプト代替**: `#!/usr/bin/env shermes`
- **WASI ターゲット**: TS → C → Wasm のパイプライン

ただし障害として、SH は「エンジン」であり「ランタイム」ではない(`fs`、`http` 等を自前で書く必要)、npm エコシステムが使えない、TS サポートが sound subset 限定、安定性が production レベルでない、ロングランニング steady-state は V8 に劣る、等がある。

---

## 3. 設計思想 — Spinel に学ぶ

[matz/spinel](https://github.com/matz/spinel) の重要な設計判断:

### 3.1 「禁止」じゃなく「未対応」

Spinel の制限は禁止リストではなくサポート範囲の宣言:

> No eval / No metaprogramming / No threads / No encoding / No general lambda calculus

言語仕様には手を入れず、「コンパイラができる範囲を正直に切る」だけ。利用者は普通の Ruby を書き、通れば AOT、通らなければインタプリタにフォールバック。

**TS 文脈に持ち込むと**: `any` 禁止リンターを作る代わりに、**コンパイラが多態を発見した時点で諦めるだけ**。書く側は普通の TS を書く。

### 3.2 全プログラム型推論

Spinel は Ruby に型注釈を一切足さず、全呼び出し箇所を見て型を確定。RBS や Sorbet 的な追加表記なし。

**TS 文脈に持ち込むと**: TS の型注釈はヒントとして使うが、「型注釈 = 信用」とせず**全プログラムで本当にその型しか来ないか検証**。多態が見つかれば「ここで `arr.push(x)` の x が `string | number` です」とエラー。

### 3.3 Self-hosting をスコープ決定装置にする

Spinel のバックエンドは Spinel 自身がコンパイルできる Ruby サブセットで書かれている。「**自分自身をコンパイルできる範囲が、サポート範囲の下限**」。仕様議論ではなく実装作業がサブセットを規定する。

### 3.4 C 出力 + 単一ヘッダランタイム

- 21K 行の Ruby(codegen) + 600 行の C ヘッダ(runtime) + 必要に応じて bigint/regexp
- 生成バイナリの実行時依存は libc + libm のみ
- LLVM IR ではなく C を選んだ点が現実的(デバッグ・移植性で有利)

### 3.5 ベンチマーク

74 feature tests / 55 benchmarks pass。geometric mean で miniruby の **~11.6x**。fib, ackermann, mandelbrot, Conway's GoL など計算系で 30–80x、データ構造系で 2–20x、実アプリ系で 3–10x。**個人プロジェクトでこの成果**であることが、TS 版の野心の上限を引き上げてくれる。

---

## 4. MVP スコープ

### 4.1 サポートする TS 機能(全 Phase 通算の目標)

- クラス、`extends`、`implements`、メソッド、`super`
- `interface`、`type` エイリアス、基本ジェネリクス(monomorphize 可能な範囲)
- 制御フロー全般(`if`/`switch`/`for`/`while`/`break`/`continue`/`return`)
- 関数、アロー関数、クロージャ
- 基本型: `number`(`f64`)、`string`、`boolean`、`Array<T>`、`Map<K,V>`、`Set<T>`
- discriminated union with exhaustive match
- `try`/`catch`/`finally`、`throw`
- ES module(`import`/`export`)、静的解決のみ
- `null | undefined` の narrowing 必須
- `console.log`、基本 I/O

各機能の現状の対応範囲は `CLAUDE.md` の「設計上の固定点」を参照。

### 4.2 未対応(コンパイルエラー)

- `any`、`unknown` の終端使用(narrowing なしで返す等)
- `as` キャスト(`as const` のみ許容)
- decorator
- `eval`、`Function` constructor、`Proxy`、`Reflect`
- prototype 改変、`__proto__`
- index signature の dynamic key
- 動的 `import()`
- `with`、`arguments` オブジェクト
- `Symbol.iterator` などのメタプログラミング(将来検討)
- async/await(Phase 2 で Fiber/コルーチン実装後)

### 4.3 コンパイラ自体が使うサブセット(self-hosting)

上のサポート範囲のうち、ジェネリクスを最小限に絞り、async/await を使わない範囲。**コンパイラがコンパイラを書けることがサブセットの最低保証**。

---

## 5. アーキテクチャ(目標)

```
TypeScript (.ts)
    |
    v
parser              Phase 0 は tsc API。Phase 1 以降の乗り換え候補は oxc。詳細 docs/parser-choice.md
    |
    v
TypedAST            型注釈付き AST
    |
    v
type inference      全プログラム推論、多態検出 → エラー
    |
    v
monomorphizer       ジェネリクスを具体化、構造的型を実質 nominal に統合
    |
    v
codegen             AST → C
    |
    v
C source (.c)
    |
    v
cc -O2 -Iruntime    標準 C コンパイラ + 自前 runtime header
    |
    v
Native binary       libc + libm のみ依存
```

現状は `parser → codegen` の 2 段で、TypedAST / type inference / monomorphizer は codegen 内に同居している。Phase 1.4c-2(generic function)以降、`Array<Box<number>>` のような型表現が必要になった時点で TypedAST を切り出す予定。

### 各層の規模見込み(Spinel 比較)

| コンポーネント | Spinel | TS 版予想 |
|---|---|---|
| Parser | 1,061 行(libprism wrapper) | SWC/oxc 借用、~500 行のラッパー |
| Codegen | 21,109 行 Ruby | 25,000–30,000 行 TS |
| Runtime header | 581 行 C | 1,500–2,000 行 C |
| Bigint | 5,394 行 C | 必要時のみ、流用検討 |
| Regexp | 1,759 行 C | 必要時のみ |

### 構造的型の扱い

TS の構造的部分型は Ruby のダックタイピングとも Java/C# の名義型とも異なる。実装オプション:

1. **monomorphize 全振り**: 各呼び出し箇所で specialize、バイナリ膨張するが速い(Rust 方式)
2. **vtable で抽象化**: interface を vtable に、小サイズ・間接呼び出し(Go interface 方式)
3. **強制 nominal**: 構造的型互換を許さず明示的キャスト要求(実装楽だが TS らしさ減)

**現状の方針**: interface は fat pointer + per-(I, C) vtable で 2 に近い形に倒し(Phase 1.4b)、コンテナの要素は monomorphize する 1 のハイブリッド。構造的型互換は EXACT match 限定で、変則的な構造的サブタイプ(covariant / contravariant)は将来課題。

---

## 6. ロードマップ(残タスク)

### Phase 1: Self-hosting 可能なサブセット

- [x] **1.1〜1.4c-3** — 制御フロー / `boolean` / `string` / shortest number / `switch` / `Array<T>` / `Map<K,V>` / `Set<T>` / `class` / `interface` / コンテナの class / interface 要素 / generic function / generic class まで着地済み。`TopazType` の structured 表現への移行も 1.4c-3 と同じタイミングで PoC として完了(`Array<Box<number>>` のようなネストが書けるようになった)。詳細は `CLAUDE.md` 参照。
- [x] **1.5-1** — 例外(`throw` / `try` / `catch`)。setjmp/longjmp + linked-list frame stack。throw 値は class instance に限定、catch binding は `catch (e: ClassName)` の明示型注釈必須、`finally` と try body 内 return / break / continue は未対応(1.5 残作業として cleanup と一緒に解禁する)。
- [x] **1.5-2** — ES module 静的解決。`src/loader.ts` で root から DFS、相対パス specifier (`./foo` / `../foo` / `.js` 拡張子は `.ts` に置換) のみ受け入れ、循環検出 (visiting set)、topological 順の `SourceFile[]` を codegen に渡す。codegen は `emit(SourceFile[])` で全 module の宣言を単一 global namespace に flatten。`export class` / `export function` / `export interface` 修飾子のみ受理、`export default` は明示エラー。**非 root module は宣言のみ許可**(top-level の executable statement は明示エラー、`main()` body は root の top-level のみ)。`import` の構文受付は `import { a, b } from "./x.js"` と side-effect `import "./x.js"` のみ、default / namespace / rename / `import type` は loader 側で明示エラー(parser 戦略 1.5-6 まで保留)。生成 C は引き続き単一 TU。`examples/module_basic_{shapes,util,main}.ts` で success、`examples/module_cycle_{a,b}.ts` で循環検出を回帰。
- [ ] **1.5 残り** — 以下を 1.5-N サブフェーズに分けて進める。順序は self-hosting に必要なものから逆算で入れ替える。詳細な棚卸し根拠は `docs/archive/self-hosting-inventory.md`。
  - [x] **1.5-3** — 全プログラム型検証層 + **discriminated union narrowing** + **`T \| undefined` narrowing** + strict field init + catch `unknown` narrowing。1.5-3a〜f まで完了。式単位の `inferType` を TypedAST 層に切り出し、多態(同じ識別子が異なる型で使われる)を検出してエラー、`switch (t.kind)` で discriminated union を狭める narrowing(`TopazType` 自身がこの形なので self-hosting の中核)、ヘルパ関数戻り値の `T | undefined` を狭める narrowing、`Map.get` の戻り値を `V | undefined` に変えて narrowing 必須化、`--strictPropertyInitialization` 相当の class field 未初期化検査、catch binding の `unknown` narrowing(これが入ると `catch (e: ClassName)` の型注釈を optional にできる)。
    - [x] **1.5-3a** — strict field init。`collectClassMembers` の末尾で `verifyDefiniteFieldInit(info)` を呼び、ctor body の top-level の `this.f = ...` 代入を集めて全 field がカバーされているかチェック。制御フロー (if/for/while/try) 内の代入は保守的に「無代入」扱い(1.5-3d の flow narrowing 基盤投入時に flow-sensitive 版に置き換える)。generic class の monomorph (`infoOverride`) は同じ ctor decl を見るので skip。`examples/strict_field_init_fail.ts` を負例として `run_fail_case` で回帰、既存 17 ケースは全て pass。
    - [x] **1.5-3b** — TopazType に union/optional variant 追加。`{ kind: "undefined" }` と `{ kind: "union", variants }` を導入(現状は `T | undefined` 限定、scalar | undefined は 1.5-3c で開ける)。`makeUnion` で flatten + dedup + canonical sort、`typeEq` は sorted variants の positional 比較、`cTypeName` は reference T → `T *` (NULL が undefined sentinel)・interface T → `topaz_iface_<I>` (`.data == NULL` が sentinel) で union 全体を inner の C 表現に collapse、scalar | undefined はエラーで reject。`emitWithExpected` が `undefined` 識別子を NULL / fat pointer compound literal に lower、`applyCoercion` は T → `T | undefined` widening を no-op で通す。`===` / `!==` は `typesOverlap` ベースに変更し、片方が `undefined` の場合だけ専用 lowering (interface は `.data` 比較)。narrowing 未実装なので union 値の field 参照・method call・`console.log` は明示エラーで reject (1.5-3d で narrowing が入るまで)。`examples/optional_basic.ts` で positive、`examples/optional_field_access_fail.ts` で negative 回帰。
    - [x] **1.5-3c** — Map.get の戻り値を V | undefined 化。`runtime/runtime.h` に scalar の sentinel struct `topaz_opt_<scalar>` (`{ present, value }`) と `topaz_opt_wrap_*` / `topaz_opt_absent_*` / passthrough マクロを追加、`TOPAZ_MAP_DEFINE` に `(opt_t, opt_wrap, opt_absent)` の 3 引数を追加して `_get` を opt 戻りに変更。codegen 側で `cTypeName` に scalar | undefined → `topaz_opt_<scalar>` 分岐、identifier emit で narrowed scalar opt のとき `.value` 経由、`emitMapMonomorphMacro` を 6 引数化(class V は passthrough + NULL、iface V は passthrough + 専用 `topaz_iface_<I>_absent` マクロ)、`emitUndefinedLiteral` / `applyCoercion` / `===` undefined lowering に scalar opt 対応を追加、`inferType` の `Map.get` を `makeUnion([V, T_UNDEFINED])` に変更。1.5-3d の narrowing 機構経由で `if (x !== undefined)` で value として使える。`examples/map_set_basic.ts` と `examples/map_set_class_iface.ts` を narrowing パターンに書き換え、新規 `examples/optional_map_get.ts` を追加。25 ケース全て pass。
    - [x] **1.5-3d** — `T | undefined` の flow-sensitive narrowing。`Scope` に `narrowings` overlay を 1 層追加(`stack` と並走させて `push`/`pop` 同期、`lookup` が narrowing を優先)、`extractNarrowing(cond, polarity)` が `x === undefined` / `x !== undefined`(左右どちらでも、変数側は単一識別子)を `{ name, type }` に解釈。`IfStatement` ハンドラが then / else 両方の polarity で narrowing を抽出して `emitStatementAsBlock` の optional 引数で渡し、ブランチ内 scope に install。`emitBlock` は各 statement の後で `applyCarryNarrowing` を呼び、「then が必ず exit する `if`」「else が必ず exit する `if`」を見つけたら以降の statement に inverted narrowing を残す(`alwaysExits` は return/throw/break/continue/block 末尾再帰/両 branch exit する if を保守的に判定)。`examples/optional_narrow.ts` で then/else narrowing、early-return narrowing、interface narrowing、throw 経由の cleanup などを 10 行カバー。**未対応**: narrowed scope 内での widening 再代入(narrow 型に対する型チェックでエラー)、compound condition (`&&` / `||`)、type guard function、`instanceof`、`in` operator、property の narrowing(1.5-3e/1.5-3f で必要に応じて追加)。1.5-3a の definite-assignment 検査の flow-sensitive 化は次の作業候補だが、現状の保守実装で src/ / examples すべて通っているため、self-hosting で踏むまで延期。
    - [x] **1.5-3e** — discriminated union narrowing。class union (`Circle | Square`) を `{ kind: "circle", radius } | { kind: "square", side }` の string literal discriminator で同定し、`switch (s.kind) { case "circle": /* s narrows to Circle */ }` で各 case 内 narrow。C 表現は fat pointer `topaz_dunion_<sorted>_or_<...> = { topaz_string kind; void *data; }`、`emitWithExpected` で class → dunion 構築、identifier emit で narrowed dunion → 元 class へ cast。`examples/dunion_basic.ts` で positive、`examples/dunion_field_access_fail.ts` で「narrow せず field 参照したらエラー」を回帰。
    - [x] **1.5-3f** — 多態検出 + catch binding の `unknown` narrowing。TopazType に `{ kind: "unknown" }` を追加、`typeFromAnnotation` で `UnknownKeyword` を受理、`cTypeName` は `void *`。catch binding の型注釈を optional 化(`: unknown` または無し → `T_UNKNOWN`)、`: ClassName` は引き続き受理。`instanceof ClassName` を binary operator として追加、AST 上で右辺は宣言済み class 名識別子限定。RTTI は全 class struct の先頭に `const char *__topaz_class_tag` フィールドを追加(constructor で per-class static sentinel `topaz_class_<C>_tag` のアドレスを書く)、`x instanceof C` を `*((const char * const *)x) == &topaz_class_<C>_tag` に lower (NULL guard 付き stmt expr)。`extractNarrowing` を拡張して `<id> instanceof <Class>` の positive 分岐で `unknown` → 当該 class に narrow。narrowing 後の identifier emit は `void *` → `topaz_class_<C> *` に cast。多態検出は既存の `inferType` 型一致チェックで実質的にカバー済み(同 identifier の異型再代入は assignability check で reject)、本サブフェーズで追加コードは無し。`examples/catch_unknown.ts` で positive、`examples/catch_unknown_unnarrowed_fail.ts` で「narrow せず field 参照したらエラー」を回帰。これで 1.5-3 完了。
  - [ ] **1.5-3.5 (新設)** — **syntactic sugar 集中投入**。`src/codegen.ts` の棚卸しで 1.5-3 単独では self-hosting に届かないことが判明したため、別サブフェーズに切る。中身: `for-of`(58 箇所、**Array<T> は 1.5-3.5b、Set / Map.values() / Map.keys() は 1.5-3.5g-mapset で着地**、destructuring 必須の `.entries()` は destructuring サブステップ待ち)、arrow function + closure キャプチャ(22 箇所、`Array.map` のため、**1.5-3.5e で着地**)、template literal(280 箇所、**1.5-3.5a で着地**)、destructuring、optional chaining(`?.`、**1.5-3.5d で着地**)、nullish coalescing(`??`、**1.5-3.5c で着地**)、non-null assertion(`!`、**1.5-3.5c で着地**)、spread(`...x`、関数 args 渡しのみ)、`Array.map` / `.filter` / `.join` / `.includes` / `.slice`(**1.5-3.5f で着地**)、`Map.values()` / `Set.values()`(**1.5-3.5g-mapset で着地**、Iterator interface 経由の bare 利用は **1.5-3.5g-iterator** サブステップ待ち)。**1.5-4 (ヒープ管理)と一緒に検討する**: template literal を `+` に lowering すると毎回 malloc が走るため、arena の前にこれを入れると leak が劇的に増える(→ arena 着地後の 1.5-3.5a で着手して回避)。
    - [x] **1.5-3.5a** — template literal の `topaz_string_concat` chain への lowering。`runtime/runtime.h` に `topaz_number_to_string` / `topaz_boolean_to_string` を追加(number は既存 `topaz_emit_number_shortest` を arena 48-byte buf に書き出す版へ refactor、boolean は `static const char[]` 戻り)、`topaz_console_log_number` も `topaz_number_to_string` 経由に統一して format 回帰テストを既存 `number_format` smoke に集約。codegen 側で `ts.isTemplateExpression` を `emitExpression` / `inferType` の `isStringLiteral` 隣に追加し、`emitStringLiteral` の signature を `TemplateHead` / `TemplateMiddle` / `TemplateTail` も受けるよう広げて escape ロジックを流用。各 `${}` substitution は number / boolean / string のみ受理(class / interface / array / map / set / unnarrowed `T | undefined` は明示エラー)、空フラグメントは skip するので `` `${name}` `` は concat 呼び出し 0 回まで折り畳む。新規 `examples/template_literal.ts`(positive、20 行)と `examples/template_literal_unsupported_fail.ts`(class instance 埋め込みエラー)、30 ケース全 pass。決定ログは `docs/archive/implementation-log.md` 末尾。次は arrow + closure と `for-of` を `Array.map` のために組で(closure キャプチャの C 表現と iterator interface 設計の判断が同時に立つ)。
    - [x] **1.5-3.5b** — `for-of` over `Array<T>` の index-based for-loop への lowering。`emitStatement` に `ts.isForOfStatement` 分岐を追加、RHS を `topaz_array_<short> *` の tmp に snapshot して `for (size_t i = 0; i < arr->len; i++)` で反復、body 先頭で `<elem> bindName = arr->data[i];` を declare(bounds check は `i < len` で構造的に不要なので `_at` は使わず direct buffer 参照)。binding は `const` / `let` の単一識別子のみ、destructuring・初期化子付き・複数宣言・`var`・`for await` は明示エラー、型注釈は elem と EXACT 一致。RHS が Map / Set / string の場合は hint 付きエラーで reject(`Map.values()` / `Set` iteration / string indexing は 1.5-3.5g で Iterator interface と一緒に)。`for-in` は構文丸ごと未対応。新規 `examples/for_of_array.ts`(positive、number / boolean / string / class / interface / break / continue / nested / 空 / `let` rebind / array literal RHS の 17 行)、`examples/for_of_map_fail.ts`(Map RHS reject)、`examples/for_of_destructuring_fail.ts`(destructuring reject)、合計 33 ケース全 pass。決定ログは `docs/archive/implementation-log.md` 末尾。
    - [x] **1.5-3.5c** — `!` non-null assertion と `??` nullish coalescing の lowering。`inferType` / `emitExpression` にそれぞれ `ts.isNonNullExpression(expr)` 分岐と `ts.SyntaxKind.QuestionQuestionToken` 分岐を追加。`!` は operand を tmp に snapshot して sentinel slot を check(scalar = `topaz_opt_T.present`、class = `T *` NULL、iface = fat pointer `.data == NULL`)、失敗時は `fputs + abort`。`??` は同じ sentinel 上の short-circuit ternary で、scalar はチェーン用に RHS が `T | undefined` の場合は present branch を `__t` のまま yield(結果型 `T | undefined`)、T の場合は `__t.value`(結果型 T)を yield する分岐を入れた。class / iface は両ブランチが同じ pointer / fat pointer なので分岐不要。RHS は `emitWithExpected` 経由で class → iface coercion / 文字列リテラル → string widening が自動。no-op `!` / `??`(既に `T` 型に対して適用)は明示エラー(TS は warning だが Topaz は assertion / fallback の意味が消えるため厳格に止める)。新規 `examples/non_null_and_coalesce.ts`(scalar / class / iface の `Map.get` 経由 + 明示 `T | undefined` binding + class→iface coercion fallback + 二段チェーン + 関数中の `!` の 18 行)、`examples/non_null_non_optional_fail.ts` と `examples/coalesce_non_optional_fail.ts` で no-op reject、合計 36 ケース全 pass。決定ログは `docs/archive/implementation-log.md` 末尾。次は **1.5-3.5d (optional chaining `?.`)** → 1.5-3.5e (arrow + closure)→ 1.5-3.5f (`Array.map` 系)→ 1.5-3.5g (`Map.values()` + 最小 Iterator interface)。
    - [x] **1.5-3.5d** — optional chaining `?.` の 3 形態(`a?.b` / `a?.b()` / `a?.[i]`)を `T | undefined` 受信者専用の lowering として投入。`f?.()` optional call は明示 reject(arrow + closure 1.5-3.5e まで保留)。決定ログは `docs/archive/implementation-log.md` 末尾。
    - [x] **1.5-3.5e** — arrow function `(p: T) => R` + closure キャプチャ。fn 型 `{ kind: "fn", params, returnType }` を TopazType に追加、C 表現は fat pointer `{ R (*fn)(void *env, ...); void *env; }` で per-arrow に env struct を生成。capture 解析は `Scope` の barrier 機構(closure 境界で外側 frame を lookup から隠す + `lookupAcrossBarrier` で capture 候補だけ覗き見る)で実装、by-value (scalar copy / class & iface は pointer 共有) で env struct に詰める。reference type は JS の by-reference に近い semantics、scalar は **per-arrow snapshot で divergence**(`counter = 999` 後に呼び出しても初期化時の値を見る)。per-iteration env alloc により for-of 内 arrow の loop var freshness は自動解決。`emitWithExpected` 経由で param 型注釈無し arrow への contextual typing を許容、独立式中の arrow は param + return 注釈必須。**未対応**(明示エラー): 関数値を Array / Map / Set に格納(1.5-3.5f で `Array.map` 着地と一緒)、nested fn type を fn param / return 位置に書く、generic arrow、async arrow、`f?.()` optional call、function expression。新規 `examples/arrow_basic.ts`(18 行 positive、capture-by-value divergence の確認含む)、`arrow_unannotated_fail.ts` / `arrow_in_container_fail.ts` / `arrow_nested_fn_type_fail.ts` で error path、合計 43 ケース全 pass。決定ログは `docs/archive/implementation-log.md` 末尾。次は **1.5-3.5f (`Array.map` / `.filter` / `.join` / `.includes` / `.slice`、closure を Array storage に格納)** → 1.5-3.5g (`Map.values()` + 最小 Iterator interface)。
    - [x] **1.5-3.5f-map** — `Array<T>.map<U>(fn: (e: T) => U): Array<U>`。1.5-3.5e で確定した arrow + fat pointer 基盤の上に最初の higher-order Array メソッドを乗せた。**lowering**: stmt-expression 内で receiver / callback / dst を tmp 化 → `topaz_array_<dst>_reserve(dst, src->len)` で事前確保 → index for-loop で `dst.push(cb.fn(cb.env, src->data[i]))` を反復 → 最後に `dst` を yield。**callback inference**: 新規 `inferCallbackFn(cb, [elem], "Array.map")` ヘルパが (a) arrow リテラルなら param annotation を expected elem 型と EXACT 一致 check(annotation 無しは contextual に補完)、(b) expression body なら param を tmp scope に push して `inferType(body)` で return type を後付け推論、(c) block body なら return annotation 必須(複数 `return` の型 union が `U` を不確定にするので 1.5-3.5f-map では reject、1.5-3.5g 以降で flow-sensitive 集約の余地)、(d) identifier (fn-typed binding) なら既存 `inferType` で fn 型を確認して arity / param 型を check。**callback return 型の制約**: fn 型 / `T | undefined` / undefined を含む union は reject(`Array<fn>` / `Array<T | undefined>` の monomorph がまだ無いため)、scalar / class / iface / nested container は OK。**`recordArrayMonomorph(result)` + `recordFnMonomorph(fnType)`** を `.map` 経路の両方(`emitArrayMethodCall` と `inferType` の `isPropertyAccessExpression` 経路)で呼び、dst Array typedef と callback fn typedef の取りこぼしを防ぐ。**Array<fn> 解禁は今回見送り**: `.map` は callback を引数で受けて呼び出すだけで Array storage に詰めない、つまり container 側を解禁する必要が無い。`Array<fn>` 自体は 1.5-3.5g 以降の宿題。**未対応**(明示エラー): block body callback で return annotation 無し / arity mismatch / param 型不一致 / callback return が fn or `T | undefined` or undefined-union / `.map` 引数が 0 個 or 2 個以上 / destructuring・optional・default・rest param / async callback。新規 `examples/array_method_map.ts`(positive、11 ケース)、`array_map_callback_arity_fail.ts` / `array_map_callback_param_mismatch_fail.ts` / `array_map_block_no_annotation_fail.ts` で error path を 3 本固定、合計 47 ケース全 pass、既存 43 ケース(template_literal / for_of_array / arrow_basic 含む)に regression 無し。決定ログは `docs/archive/implementation-log.md` 末尾。次は `.filter` / `.includes` / `.slice` / `.join` を順次着地させて 1.5-3.5f を完了させる。
    - [x] **1.5-3.5f-filter** — `Array<T>.filter(fn: (e: T) => boolean): Array<T>`。**lowering**: stmt-expression 内で receiver / callback / dst を tmp 化 → index for-loop で `<elem ty> __e = src->data[i]; if (cb.fn(cb.env, __e)) dst.push(__e);` を反復 → 最後に dst を yield。dst elem 型 = src elem 型なので **既存 Array monomorph を流用**(新規登録不要)、`reserve` は filter 後の要素数が不明なので省略(doubling alloc に任せる)、`__e` を一時変数化して `src->data[i]` の二重評価を避ける(callback への引数と push 引数の両方で参照)。**callback 制約**: `.map` の `inferCallbackFn(cb, [elem], "Array.filter")` ヘルパをそのまま流用、return 型は `boolean` EXACT 一致のみ受理(truthy / falsy coercion は一切採用しない、TS strict と同方針 / `if` / `while` 条件の strict boolean 要件とも整合)、block body の return annotation 必須要件は `.map` から継承(複数 return union 回避)。**`recordFnMonomorph(fnType)` のみ呼び**(`recordArrayMonomorph` は src と dst が同一型なので不要)、callback fn typedef の取りこぼしを防ぐ。**未対応**(明示エラー): callback return が `boolean` 以外、`inferCallbackFn` で reject される arity mismatch / param 型不一致 / destructuring・optional・default・rest param / block body の return annotation 欠落、`.filter` 引数 0 個 / 2 個以上、async callback。新規 `examples/array_method_filter.ts`(positive、scalar number / string / boolean / class elem への filter、annotated callback、fn-typed identifier 経由、scalar capture (threshold)、empty 配列、全要素 reject、chained `.filter().filter()` / `.filter().map()` の 12 ケース)、`array_filter_callback_non_boolean_fail.ts` で callback non-boolean return を reject、合計 49 ケース全 pass、既存 47 ケース(`.map` 含む)に regression 無し。決定ログは `docs/archive/implementation-log.md` 末尾。次は `.includes`。
    - [x] **1.5-3.5f-includes** — `Array<T>.includes(target: T): boolean`。**lowering**: `({ <src ty> __src = recv; <T> __t = target; bool __r = false; for (size_t __i = 0; __i < __src->len; __i++) { if (<elem-eq>) { __r = true; break; } } __r; })` の stmt-expression、receiver と target を 1 度ずつ tmp 化、break で早期脱出、後付けで `__r` を yield。**`<elem-eq>` の inline 展開**(elem 種別ごと): (a) `number` → `((__src->data[i] == __t) || (__src->data[i] != __src->data[i] && __t != __t))` で SameValueZero(後半が NaN-NaN 一致)、(b) `boolean` → `__src->data[i] == __t`、(c) `string` → `topaz_string_eq(__src->data[i], __t)`、(d) class → ポインタ `==` 比較(Set の reference identity と同期)、(e) iface → `.data` ポインタ `==` 比較(fat pointer の data field、同じく reference identity)。**SameValueZero 採用**: ECMA-262 `Array.prototype.includes` 仕様、Map / Set の key 等値とも整合、`xs.includes(NaN)` が true を返す(JS の `===` の divergence)。**callback 不要、signature がシンプル**: `target` 型が elem 型と EXACT 一致を要求(`emitWithExpected` で class → iface coercion は自動、scalar 間の coercion は無し)。**runtime.h に新規ヘルパ追加せず inline 展開**: scalar number の SameValueZero は 4 演算で書けるので runtime.h を肥大化させない方針と整合、複数 `.includes` 呼び出しでも `__src` / `__t` tmp が独立に生成されるので衝突無し。**未対応**(明示エラー): 第 2 引数 `fromIndex`(JS 仕様の `(target, fromIndex)`、`xs.slice(fromIndex).includes(target)` で代替可)、`target` 型が elem 型と不一致、elem が Array / Map / Set / fn / undefined / union(等値定義が未確定)。新規 `examples/array_method_includes.ts`(positive、scalar number の hit / miss / NaN 自己一致(`0/0` で NaN 生成)/ string の hit / miss / boolean の両値 hit / class instance の reference identity hit + 別 instance miss / iface fat pointer の `.data` identity hit + 別 instance miss / 空配列 miss / 単一要素配列 hit + miss / `.filter().includes()` chain / `.map().includes()` chain の 19 ケース)、`array_includes_type_mismatch_fail.ts`(target が elem と不一致)、`array_includes_from_index_fail.ts`(`fromIndex` 第 2 引数 reject)で error path を 2 本固定、合計 53 ケース全 pass、既存 51 ケース(`.filter` 含む)に regression 無し。**`NaN` global の代用**: 現状 `NaN` 識別子は未対応(`global Object` の properties はサポート外)、テストでは `0 / 0` で NaN を生成(IEEE 754 で 0/0 は NaN、C 側でも同じ挙動)。決定ログは `docs/archive/implementation-log.md` 末尾。次は `.slice`。
    - [x] **1.5-3.5f-slice** — `Array<T>.slice(start?: number, end?: number): Array<T>`。**lowering**: stmt-expression 内で receiver / raw start / raw end / dst を各 1 回 tmp 化、`__lo` = `topaz_slice_normalize(__raw_start, __len, 0)` / `__hi` = `topaz_slice_normalize(__raw_end, __len, __len)` で範囲を size_t に正規化、`__hi < __lo` なら empty に clamp、`topaz_array_<src>_reserve(__d, __hi - __lo)` で事前確保(range が確定するので doubling alloc を 1 回も発生させない)、index for-loop で `dst.push(src->data[i])` を反復。**`topaz_slice_normalize(double n, size_t len, size_t def)`** を `runtime/runtime.h` に追加: `isnan(n)` で undefined default、`n < 0 ? (double)len + n : n` で負数を折り畳み、`[0, len]` に clamp、最後に `size_t` に narrow(JS の `Array.prototype.slice` 正規化を 1 関数に集約、NaN sentinel で `undefined` default を表現、`<math.h>` の `isnan` / `NAN` は既に include 済み)。**`undefined` default の NaN sentinel 表現**: 省略された start / end は呼び出しサイトで `(double)NAN` をリテラル展開、ヘルパ内で `isnan` check して default(`0` / `len`)に置換 — 引数有無で別の helper を持つ必要がなくなる。**dst monomorph = src monomorph**: `.filter` と同じく要素型変換しないので `recordArrayMonomorph` 不要、callback 無しなので `recordFnMonomorph` も不要(完全に callback-less メソッド)。**引数型制約**: 各引数を `inferType` で `number` EXACT 一致 check、不一致は call site で reject。**未対応**(明示エラー): 引数 3 個以上、引数型が `number` 以外、`Array<T>` 以外の receiver(string slice は未対応)。新規 `examples/array_method_slice.ts`(positive、両端指定 / start のみ / 無引数全コピー / 負数 start / 負数 end / 負数両方 / start > end で empty / start > len で empty / end > len で clamp / 空 source / string elem / class elem の reference identity 保存 / chained `.slice().slice()` / `.filter().slice()` / slice 後の独立性(push の影響範囲)の 15 ケース)、`array_slice_arg_type_fail.ts` / `array_slice_too_many_args_fail.ts` で error path を 2 本固定、合計 55 ケース全 pass、既存 53 ケース(`.map` / `.filter` / `.includes` 含む)に regression 無し。**`.includes` の `fromIndex` 代替が確立**: `xs.slice(fromIndex).includes(target)` で代替可能、`.includes` の `fromIndex` 第 2 引数 reject は維持。決定ログは `docs/archive/implementation-log.md` 末尾。次は `.join`。
    - [x] **1.5-3.5f-join** — `Array<T>.join(separator?: string): string`。**lowering**: stmt-expression ではなく **per-monomorph `topaz_array_<src>_join(src, sep)` ヘルパ関数** を codegen で emit、call サイトは `topaz_array_<name>_join(recv, sepStr)` の plain function call 1 行に lower(receiver / sep は C の引数評価規則で 1 度ずつ評価、stmt-expression による tmp 化は不要)。**ヘルパ生成戦略**: `TOPAZ_ARRAY_DEFINE` マクロを汚さない方針で、新規 slot `arrayJoinHelperSlot`(`containerMonomorphSlot` の直後、`fnTypedefSlot` の前)に `static inline topaz_string topaz_array_<elem>_join(topaz_array_<elem> *src, topaz_string sep)` を吐く。`recordArrayJoinMonomorph(baseType)` が `.join` 呼び出しサイトで Map に登録、emit 末尾で worklist を drain、複数呼び出しで dedupe(Map key は `typeKey`)。**ヘルパ本体は 2 パス**: pass 1 = `n - 1` 個の separator 長 + 全 elem 長を accumulate して `total` を確定(`n == 0` の early return で `(topaz_string){"", 0}`)、pass 2 = `topaz_arena_alloc(total + 1)` で 1 度 alloc して order どおり書き込む(separator → elem)、末尾 `'\0'` も焼く(`printf("%s", ...)` 互換)。**elem stringify は pass 1 / pass 2 両方で呼ぶ**(中間 cache を持たない方針、`number` 1000 要素で 48KB の余分 alloc は per-process arena の前で誤差レベル、cache 設計の複雑さの方がコスト高): `number` → `topaz_number_to_string`、`boolean` → `topaz_boolean_to_string`、`string` → identity。**separator default**: 引数省略時は call サイトで `((topaz_string){",", 1})` の compound literal を直接合成、ヘルパは optional 引数を意識しない。**未対応**(明示エラー): 引数 2 個以上(`Array.join expects at most one argument`、TS は無視するが Topaz は厳格)、elem が scalar 3 種以外(class / iface / Array / Map / Set / fn — `.toString()` 解禁時に再評価、現状は `.map((x) => ...)` で string 化してから join)、separator が string 以外(`Array.join separator must be string`、number → string coercion は入れない方針)。新規 `examples/array_method_join.ts`(positive、number elem の default / explicit / 空 / multi-char separator / string elem / boolean elem / 空配列 / 単一要素 / ECMA-262 number format / `.map().join()` / `.filter().join()` / `.slice().join()` chain / `+` で外側 concat / 複数 `.join` の monomorph 共有の 23 ケース)、`array_join_class_elem_fail.ts` / `array_join_sep_type_fail.ts` / `array_join_too_many_args_fail.ts` で error path を 3 本固定、合計 59 ケース全 pass、既存 55 ケース(`.map` / `.filter` / `.includes` / `.slice` 含む)に regression 無し。**Phase 1.5-3.5f closed**: Array 系 higher-order メソッド 5 種(map / filter / includes / slice / join)の最小セットが揃った。決定ログは `docs/archive/implementation-log.md` 末尾。次は **1.5-3.5g** = `Map.values()` / `Set.values()` + 最小 Iterator interface(for-of を container hard-code から iterator 経路へ昇格、`Array<fn>` storage も同時に解禁)。
    - [x] **1.5-3.5g-mapset** — `for-of` over `Set<T>` / `Map.values()` / `Map.keys()` / `Set.values()` / `Set.keys()`。**1.5-3.5g** は (a) **mapset サブステップ**(本項 = 直接 slot 走査)、(b) **Iterator interface サブステップ**(`Iterator<T>` の最小 trait + `next()` 経由 dispatch、`.map` 結果を直接 for-of の RHS に置く path、`.values()` の bare 利用解禁)、(c) **`Array<fn>` storage**(callback 列を Array に詰める)の 3 つに分割、mapset を最初に着地。既存の Map / Set monomorph(open-addressing + slot state machine)を再利用、runtime.h 無修正。**lowering**: 生成 C は `{ <ht_c_ty> __topaz_for_ht_<id> = <recv>; for (size_t __topaz_for_idx_<id> = 0; __topaz_for_idx_<id> < ht->cap; __topaz_for_idx_<id>++) { if (slots[i].state != TOPAZ_HASH_SLOT_OCCUPIED) continue; <bind_c_ty> bindName = slots[i].<field>; <body>; } }` の cap-based walk + OCCUPIED 以外 skip。recv は tmp に 1 度 snapshot、`cap == 0` の empty Map / Set は body に入らず safe(`slots` が NULL でも触らない)、tombstone(`DELETED`)は state check で自然 skip、user の `continue` / `break` も同じ C for-loop に乗るので semantics が自然合成される。**dispatch 経路**: `emitForOfStatement` で 4 経路 — (1) RHS が `.values()` / `.keys()` の special form → `emitForOfHashLowering(field)` を呼ぶ、(2) RHS が `.entries()` → `for-of over .entries() is unsupported (requires destructuring binding, not yet implemented)` で reject(destructuring 解禁時に同時着地)、(3) RHS の `inferType` が `set` → `emitForOfHashLowering(field: "key")` を呼ぶ(plain Set RHS と `.values()` / `.keys()` は同一 walk)、(4) RHS の `inferType` が `array` → 既存 `emitForOfArrayLowering`。special form 判定は `inferType(rhs)` より先に行う(`.values()` の戻り値型を持たないので inferType に乗せると混乱)。**bare 利用 reject**: `const vs = m.values()` / `console.log(m.values())` 等の for-of 外利用は `Map.values() is only allowed as the right-hand side of a for-of statement` で reject(Iterator interface サブステップ着地で解禁予定)、`emitMapMethodCall` / `emitSetMethodCall` / `inferType` の Map/Set method dispatch の 3 箇所で同一 message を返す。**Set / Map の非対称性**: Set bare RHS は OK、Map bare RHS は引き続き reject(JS では `[K, V]` tuple yield、destructuring 必須)— JS 仕様そのままで documented divergence ではない。**class / iface element**: Set<class> / Map<scalar, iface> 等の monomorph は 1.5-3a 経路で既に登録済み、binding は既存の `topaz_class_<C> *` field access / fat pointer vtable getter にそのまま下りる。**回帰**: positive 2 本(`for_of_set.ts` 11 ケース、`for_of_map_values.ts` 11 ケース)、fail 2 本(`for_of_map_entries_fail.ts` / `map_values_outside_for_of_fail.ts`)、合計 64 ケース全 pass、既存 60 ケースに regression 無し。**hash order 非依存性**: 全 positive ケースは sum / count / aggregated length など可換演算で expected を確定。**新規 documented divergence**: Set / Map の iteration order は hash 順(open-addressing slot 順)で JS の insertion-ordered とは divergence(`OrderedMap<K, V>` を別 type で導入する余地は将来課題として残す)。決定ログは `docs/archive/implementation-log.md` 末尾。次は **1.5-3.5g-iterator** = `Iterator<T>` interface + `next()` dispatch(`.values()` の bare 利用 + `.map` 結果を直接 for-of RHS に置く path)→ **1.5-3.5g-array-fn** = `Array<fn>` storage 解禁 → destructuring / spread → 1.5-5 generic method / interface → 1.5-6 self-hosting。
    - [x] **1.5-3.5g-iterator** — 最小 `Iterator<T>` interface + bare `.values()` / `.keys()` の解禁。**狙い達成**: `.values()` / `.keys()` を first-class な `Iterator<T>` 型に昇格、`const vs = m.values()` / 関数引数 / 関数戻り値 / for-of 経由消費を全て同一型で扱えるようになった。**C 表現**: 候補 (a) **state struct + 関数ポインタ fat pointer** を採用 — `topaz_iter_<T> = { void *state; T (*next)(void *state, bool *done); }`、state は arena alloc、`done` は out-param。1.5-3.5e の fn 値表現と構造が揃う / closure-based より `done` flag が綺麗 / trait より per-source 監視コードが少ない。**runtime.h は無修正**(codegen が emit 末尾で per-elem typedef・per-container state struct・per-(source, container) `_next` 関数を 3 層 slot に吐く): `topaz_iter_<elem-tag>`(per-elem typedef、`iterTypedefSlot`)、`topaz_iter_state_<container-tag>`(per-container state struct、Map.values + Map.keys は state を共有 / Set は両 .values/.keys と素の RHS が全て同じ state)、`topaz_iter_<container-tag>_<values|keys>_next`(per-source `_next` 関数)。`recordIterMonomorph(elemType, containerType, source, field)` が 3 つの Map に登録、emit 末尾で 3 slot drain。**TopazType に `iter` variant 追加**: `{ kind: "iter"; elem: TopazType }`、`typeEq` / `typeKey` / `cTypeName` / `typeFromAnnotation` に対応分岐、`elemTag` は container element の reject 側で明示エラー(`Iterator<T>` は単一 pass + ownership 不定で container に詰められない)。**bare 利用の解禁**: `emitMapMethodCall` / `emitSetMethodCall` / `inferType` の 3 箇所の reject を解除、`emitIterConstruction(recvExpr, containerType, source, elemType, field)` が stmt-expression `({ topaz_iter_state_<C> *s = topaz_arena_alloc(sizeof(...)); s->src = recv; s->idx = 0; (topaz_iter_<T>){ .state = s, .next = (...)&topaz_iter_<C>_<values|keys>_next }; })` を吐く。**`Iterator<T>` 型注釈**: `typeFromAnnotation` で `Iterator<T>` を 1 type argument 強制 + elem 形状チェック(scalar / class / iface のみ)、bare 注釈だけでも typedef は emit slot に reserve される(関数 return type で使われるため)。**for-of 5 経路目**: `emitForOfStatement` で `rhsType.kind === "iter"` を新規 dispatch、`emitForOfIteratorLowering` で while-form lower `{ topaz_iter_<T> __it = recv; for (;;) { bool __done = false; <T> __v = __it.next(__it.state, &__done); if (__done) break; <T> bindName = __v; <body>; } }`。直接 walk path(1.5-3.5g-mapset の hash-form lowering = `.values()` / `.keys()` / Set RHS の direct slot walk)は引き続き存続、observationally 同一だが direct walk の方が分岐 1 段少ない。**Set / Map 対称性**: Set.values() と Set.keys() は同じ `set_values` source に集約(JS で両者が同じ関数 ref で elem yield)、`field` は常に `"key"`(Set slot は単一 key field)。**回帰**: positive 1 本(`iterator_basic.ts` = (1) Map.values() を別 var bind + for-of、(2) Map.keys() bind + for-of、(3) Set bind via .values()、(4) Set bind via .keys()、(5) 関数戻り値 Iterator<T>、(6) empty Map で iter が即終了、(7) class V Map.values() で method dispatch、(8) annotated `for (const v: number of vs)`、(9) 二重 iter の独立性、合計 10 ケース)、fail 1 本(`iterator_in_container_fail.ts` = `Array<Iterator<T>>` で elemTag reject)、合計 66 ケース全 pass、既存 64 ケースに regression 無し。**削除**: 1.5-3.5g-mapset 時の `map_values_outside_for_of_fail.ts`(bare 利用が valid になったため意味消失)。**scope 外**: `.entries()` は引き続き reject(destructuring 必須、destructuring サブステップで着地)、`Iterator<T>.map` / `.filter` / `.toArray()` 等の iter 自身のメソッドは持たない(必要なら for-of で書く)、`Symbol.iterator` の JS protocol は持ち込まない(Topaz 固有 type として扱う)、Array 系の lazy 化(`.map` を iter chain 化)も着手しない(Array 系は引き続き eager)。決定ログは `docs/archive/implementation-log.md` 末尾。次は **1.5-3.5g-array-fn** = `Array<fn>` storage 解禁。
    - [x] **1.5-3.5g-array-fn** — `Array<fn>` storage の解禁。**狙い達成**: 1.5-3.5e で arrow を fat pointer fn 値として表現できるようになっていたが、container には詰められない状態(`arrow_in_container_fail.ts` で reject)を解消、Array 側だけ解禁(Map / Set はキー等値・hash 未定義のため引き続き reject)。**C 表現**: `Array<(n: number) => number>` → `topaz_array_fn_a1__number__to__number *`、要素 1 件は `topaz_fn_a1__number__to__number = { number (*fn)(void *env, number); void *env; }`(1.5-3.5e の fn 値表現そのまま)、`TOPAZ_ARRAY_DEFINE(fn_a1__number__to__number, topaz_fn_a1__number__to__number)` で展開。**slot 順序の問題**: 既存 `containerMonomorphSlot` は `fnTypedefSlot` より **前** にあるため、Array<fn> の `TOPAZ_ARRAY_DEFINE` をそこに置くと fn typedef が未定義で C compile 失敗。**対処**: 新規 slot `arrayFnContainerSlot`(`fnTypedefSlot` の **直後**・`arrowFwdSlot` の前)を導入、`recordArrayMonomorph` で elem が fn の場合だけこちらに振り分ける(scalar / class / iface は既存 `arrayMonomorphs` をそのまま使用)。**reject 解除箇所**: (a) `arrayOf` の fn elem reject 解除、(b) `elemTag` の `case "fn"` で `typeIdent(t).slice("topaz_".length)` を返す、(c) `recordArrayMonomorph` で fn の場合は `recordFnMonomorph(elem)` + `arrayFnMonomorphs.set(...)`、(d) `emitArrayMethodCall` の Array.map branch で callback return が fn のときの reject を削除、(e) `inferType` の PropertyAccessExpression Array.map branch でも同じ reject を削除。**`emitCall` / `emitFnValueCall`**: `arr[i](args)` のような expression callee はすでに `inferType(callee).kind === "fn"` で fn fat pointer dispatch に lower されるため(1.5-3.5e のフォールバック経路)、追加の dispatch 改修は不要。**contextual typing**: `expectType` に `ts.isArrowFunction(expr) && expected.kind === "fn"` 分岐を追加、`inferArrowType(expr, expected)` で contextual に param 型を補完(annotation 無し `(n) => n + 1` が `arr.push((n) => n + 1)` で通る)。**`inferType` 識別子の fn 解決**: `scope.lookup` 失敗時に `functionSigs` を確認して fn 型を返すフォールバックを追加(named function を fn 値として参照する経路)。**残った 1.5-3.5e 制限**: nested fn type を arrow return position に書く(`(x) => (n) => n + x` の curry)は引き続き reject、`Array<fn>` 自体は valid だが `.map` 経由で fn を返す callback を作る経路は `arrow_nested_fn_type_fail.ts` の reject に当たる。回避は for-of + push パターン(`array_of_fn.ts` のケース (5))。**回帰**: positive 1 本(`array_of_fn.ts` = (1) push 2 + index call、(2) `.length` + `[i] = arrow` 書き換え、(3) annotated arrow with scalar capture、(4) for-of で sum、(5) for-of + push でビルダー列、(6) `Array<fn>` を関数引数で受けて pipeline、(7) `.pop` で fn 取得して inline call、(8) 別 fn 型の独立 monomorph、合計 8 ケース)、fail 2 本(`map_of_fn_fail.ts` = `Map<string, fn>` で `no Map monomorph for key=topaz_string, value=topaz_fn_*` を確認、`set_of_fn_fail.ts` = `Set<fn>` で `no Set monomorph for element type topaz_fn_*` を確認)、合計 67 ケース全 pass、既存 66 ケースに regression 無し。**削除**: `arrow_in_container_fail.ts`(Array<fn> が valid になったので意味消失、Map / Set 方向の reject は新規 fail 2 本に切り出した)。**scope 外**: `Map<scalar, fn>` / `Set<fn>`(等値定義不定で値型として扱えない、必要なら `WeakMap<fn, V>` 相当を別 type で導入)、class / interface field に fn 型を持つ(struct 内 fn typedef 埋め込み + 循環解決が要、別 step)、`Array<fn>.includes`(reference identity を fn に持ち込むかが曖昧)、`Array<fn>.join`(format policy 未定)、curry を Array に貯める `.map` 経路(nested fn return の 1.5-3.5e 制限解除とセット、別 step)。**1.5-3.5g closed**: mapset / iterator / array-fn の 3 サブステップ全て着地。決定ログは `docs/archive/implementation-log.md` 末尾。次は **destructuring / spread**(`.entries()` 解禁とセット)→ 1.5-5 generic method / interface → 1.5-6 self-hosting。
    - [x] **1.5-3.5h-entries** — `for-of` over `Map.entries()` / `Set.entries()` の destructuring binding 解禁。**狙い達成**: 1.5-3.5g-mapset / iterator で `.values()` / `.keys()` は通っていたが、`.entries()` は destructuring 必須として reject していた。本サブステップで destructuring 全般ではなく **`.entries()` 専用の syntactic special form** として最小着地、`[K, V]` tuple 型 / `Iterator<[K, V]>` の合成型は導入せず、for-of の binding 解析と RHS が `.entries()` の組み合わせだけで lower。**スコープ確定**: `for (const [k, v] of m.entries())` / `for (const [a, b] of s.entries())` の 2 形態だけを受理、それ以外の destructuring binding(`for (const [a, b] of pairs)` のような Array RHS への destructuring、object destructuring、ネスト pattern、rest、default、property rename、要素数 ≠ 2、type annotation on pair binding)は全部 reject。**C 表現**: 1.5-3.5g-mapset の hash-walk lowering をそのまま再利用、binding を 1 つ → 2 つに拡張するだけ。`{ <ht_c_ty> __ht = recv; for (size_t i = 0; i < __ht->cap; i++) { if (slots[i].state != OCCUPIED) continue; <K> k = slots[i].key; <V> v = slots[i].value; <body>; } }`、同一 slot から `.key` / `.value` を 1 度ずつ取る形(`__ht->slots[i]` の二重評価は許容、index は size_t で再評価コスト 0)。Set.entries() は `[elem, elem]` を yield する JS semantics に合わせ、両 binding が同じ `slots[i].key` を見る(`<T> a = slots[i].key; <T> b = slots[i].key;`、`emitForOfHashLowering` の pair path で `firstField` / `secondField` を両方 `"key"` で生成)。**`parseForOfBinding(decl)` helper**: 単一 identifier binding と pair destructuring binding を tagged union (`{ kind: "single", name }` / `{ kind: "pair", firstName, secondName }`)で返す。reject 経路を集約: (a) ObjectBindingPattern → `for-of binding must be an identifier or [k, v] destructuring`、(b) ArrayBindingPattern with rest / default / property rename / nested pattern / omitted (sparse) elements / 要素数 ≠ 2 → 各々独自 message、(c) type annotation on pair binding → `for-of pair binding does not support type annotation`(両 element の C 型は RHS から確定するため annotation の意味が無い)。**`emitForOfHashLowering` の bindSpec 化**: signature を `(bindType, field, bindName)` triple から単一 `bindSpec: { kind: "single", name, field, type } | { kind: "pair", firstName, firstField, firstType, secondName, secondField, secondType }` に refactor、pair path は同 slot から 2 declaration を吐く。`.values()` / `.keys()` / 素の Set RHS は single bindSpec(`type` は elem、`field` は `"key"` or `"value"`)、`.entries()` は pair bindSpec を組む。**dispatch 改造**: `emitForOfStatement` で `parseForOfBinding` を最初に呼んで binding kind を確定 → RHS が `.values()` / `.keys()` + single → 既存 hash-form、RHS が `.entries()` + pair → 新 hash-form pair path、それ以外の組み合わせ(`.entries()` + single → `for-of over .entries() requires destructuring binding (for (const [k, v] of m.entries()))`、Array / Set / Iterator RHS + pair → `destructuring binding in for-of is only supported for .entries() on Map / Set`)は全て独自 message で reject。**bare reject path 更新**: `emitMapMethodCall` / `emitSetMethodCall` / `inferType` の Map / Set method dispatch 3 箇所の `.entries()` reject message を destructuring 必須 hint 付き(`for (const [k, v] of m.entries())`)に統一(1.5-3.5g-mapset では「destructuring 解禁時に同時着地」とだけ書いていたのを user 向け hint に格上げ)。**回帰**: positive 1 本(`for_of_entries.ts` = 13 ケース — (1) Map<string, number> k.length*v 加重和、(2) Map<number, number> k+v 和、(3) Map<string, class> method 経由の k.length*draw()、(4) Map<number, iface> vtable dispatch + k*draw()、(5) break で early exit、(6) continue で key.length===2 skip、(7) 空 Map で body 不実行、(8) `.delete()` 経由 tombstone walk、(9) ネスト entries × Array、(10) `let [k, v]` で reassign、(11) Set<number>.entries() で両 binding が同値、(12) Set<string>.entries() で string elem 経路、(13) closure capture で `Array<fn>` 構築 + per-iteration snapshot)、fail 2 本(`for_of_map_entries_fail.ts` を再用途で「`.entries()` + single binding reject」に書き換え、`map_entries_outside_for_of_fail.ts` 新規 = `.entries()` の bare 利用 reject)、`for_of_destructuring_fail.ts` を「Array RHS + destructuring binding reject」に書き換え、合計 69 ケース全 pass、既存 67 ケースに regression 無し。**hash order 非依存性**: 全 positive ケースは sum / count / aggregated length / closure 全 call の sum など可換演算で expected を確定(1.5-3.5g-mapset と同方針、insertion order に依存しない)。**scope 外**: Array RHS への destructuring binding(`for (const [a, b] of pairs)`、tuple 型がないと receive できない、tuple サポートとセットで別 step)、object destructuring(`for (const { x, y } of points)`、別 step)、`Iterator<[K, V]>` の合成型(Iterator interface に tuple をかぶせる必要、現状の単一 `_next` callback signature では表現不可)、destructuring を function param / variable binding にも解禁(より広範な destructuring サブステップで)、spread (`...x`)(別 step)。**1.5-3.5h subdivided**: entries 完了、Array / object destructuring + spread は後続の destructuring サブステップで別個に着地。決定ログは `docs/archive/implementation-log.md` 末尾。次は destructuring の残り(Array / object binding、function param destructuring、spread)→ 1.5-5 generic method / interface → 1.5-6 self-hosting。
    - [x] **1.5-3.5h-spread** — array literal 内の spread `...x` を `Array<T>` 源限定で解禁。**狙い達成**: src/codegen.ts での自己ホスティングが `[...t.variants]` / `["void *", ...t.params.map(...)]` / `[\`(topaz_class_${cls.name} *)self\`, ...sig.params.map(...)]` のような Array spread を多用している。1.5-3.5h-entries 着地時点では `emitArrayLiteral` 入口で「spread / holes in array literals are unsupported」として一括 reject していたのを、Array→Array spread だけ通す最小サブステップ。**スコープ確定**: array literal 内の `...expr` は `expr` が `Array<T>` のときだけ受理、elem 型は dst の elem 型と EXACT 一致(class→iface 等の per-element widening は spread 経路では行わない、必要なら fixed 要素で push するか pre-coerce)。**reject 維持**: `f(...args)` / `new C(...args)` の call-arg / new-arg spread、`[...set]` / `[...iter]` の非 Array 源 spread、destructuring の rest pattern (`const [a, ...rest] = xs`、`function f(...args) {}`)。OmittedExpression (sparse hole `[1, , 3]`) も引き続き reject。**C 表現**: stmt-expression `({ topaz_array_<T> *__t = topaz_array_<T>_new(); topaz_array_<T> *__sp_0 = <expr_0>; ...; topaz_array_<T>_reserve(__t, <fixed_count> + __sp_0->len + ...); /* src 順に: */ topaz_array_<T>_push(__t, <fixed>) | for (size_t __si = 0; __si < __sp_k->len; __si++) topaz_array_<T>_push(__t, __sp_k->data[__si]); ...; __t; })`。**snapshot 順序**: 全 spread 源を先頭でローカル tmp に確保 → reserve(fixed_count + Σ spread.len)→ src 順に fixed/spread を発火、fixed と spread の評価順は src 順を保ちつつ doubling alloc を 1 回も起こさない。**1 度評価**: 各 spread 源式は 1 度だけ評価され、reserve / push loop は同 tmp の `.len` / `.data` を見る(`for-of` over Array や `.filter` と同じ tmp snapshot 戦略を踏襲)。**`emitArrayLiteral` 改造**: SpreadElement の分岐を追加、`hasSpread` flag で従来の固定 push path と新 path を切り替え。**first element が SpreadElement の場合は源の `Array<T>` から `T` を抜く** ようにし、`expected` が `Array<U>` ならそちらが優先。**`inferType` の ArrayLiteralExpression パス** も対称に拡張(spread first → 源 elem、fixed first → 既存通り、subsequent spread は emit 時の type check で reject 担当)。**call-arg / new-arg spread の上流 reject**: `emitCall` と `emitNewExpression` の冒頭で `expr.arguments` を walk、SpreadElement があれば「spread in call arguments is unsupported (rewrite as a loop, e.g. `for (const x of xs) f(x)`)」/ 「spread in `new` arguments is unsupported」で reject(以前は SpreadElement が `inferType` の catch-all へ流れて「unsupported expression」になっていたのを、上流で明示メッセージに格上げ)。下流の `emitArrayMethodCall` / `emitMapMethodCall` / `emitSetMethodCall` / `emitClassMethodCall` / `emitInterfaceMethodCall` / `emitFnValueCall` / 各 identifier-call 経路は `expr.arguments` を positional に index するため、上流 guard 1 つで全 callee で SpreadElement 不在の invariant を保てる。**回帰**: positive 1 本(`spread_basic.ts` = 12 ケース — (1) number Array clone、(2) mixed fixed+spread+fixed、(3) 複数 spread 源、(4) empty spread 源、(5) spread-only with empty 源、(6) leading-spread 推論、(7) class 要素の reference identity 保存、(8) interface dst + spread、(9) `.map()` 結果の spread、(10) 連結後の `.filter()` chain、(11) 同 spread 源の二重展開で評価 1 度、(12) class→iface 推論なし dst で fixed 要素 widening + spread)、fail 5 本(`spread_call_args_fail.ts` = `f(...xs)`、`spread_new_args_fail.ts` = `new Pair(...xs)`、`spread_set_fail.ts` = `[...set]` 源、`spread_elem_mismatch_fail.ts` = string 源を number dst に spread、`spread_non_array_fail.ts` = scalar number を spread 源に)、合計 75 ケース全 pass、既存 69 ケースに regression 無し。**scope 外**: rest pattern (`const [a, ...rest] = xs`、`function f(...args)`、destructuring rest) は destructuring 全般と一緒に後続サブステップで再検討、Set / Iterator 源 spread(`Set→Array` 変換は `for-of` ループで代替可能、需要が出たら別 step で source kind を増やす)、spread 経路での class→iface 等の per-element widening(memcpy 1 発で済まなくなるため別 step)、tagged template spread / object literal spread (`{ ...obj }`、object literal 自体が未対応)。**1.5-3.5h closed**: entries + spread のセットで destructuring 系の最小実用面が揃った(Array / object destructuring binding と function param destructuring は引き続き reject、必要になり次第サブステップ追加)。決定ログは `docs/archive/implementation-log.md` 末尾。次は destructuring の残り(Array / object binding、function param destructuring、rest pattern)→ 1.5-5 generic method / interface → 1.5-6 self-hosting。
  - [x] **1.5-4** — ヒープ管理。`string +` の連結結果 / `throw` した class instance / `Array` / `Map` / `Set` の slot buffer の leak を per-process arena で一気に回収。`runtime/runtime.h` 先頭に chunk-based bump allocator (`topaz_arena_chunk` 32 bytes header + payload 16-byte aligned、初期 chunk 64KB、大きい alloc は専用 chunk)を embed、`topaz_arena_alloc` / `_calloc` / `_realloc` の 3 API を提供。runtime 内の `malloc` / `calloc` / `realloc` / `free` 呼び出しを全て arena 経由に置換(`free(slots)` は単純削除)、codegen の class allocation も `topaz_arena_calloc` に。realloc は単純な alloc + memcpy(Array doubling / Map rehash の grow しか起きないため in-place 最適化は省略、amortized は O(1) のまま、peak memory は最大 +100%)。新規 `examples/arena_stress.ts` で Array 1000 push / class 1000 new / string 200 concat / Map 500 set を総当たり、29 ケース全 pass。Set の構造的等値は self-hosting で踏むまで保留(現状 reference identity で `src/` は通る想定)。BDW conservative GC は self-hosting の範囲では過剰、per-process arena で十分(Phase 2 の async-await で per-task arena に拡張する余地は残してある)。
  - [ ] **1.5-5** — generic method(`class C { f<U>(...) {} }`)/ generic interface。`class` 全体の monomorph と method 単独の monomorph が直交するので、`classMonomorphs` の構造を method 単位に拡張する必要あり。**`src/` では未使用なので self-hosting (1.5-6) の前提ではない**ため、1.5-6 の後ろに回しても良い。
  - [ ] **1.5-6 prep** — self-hosting 着手前の地ならし。`src/codegen.ts` を Topaz 自身で食わせるために、src 内で多用されている TS 機能を no-op or 最小 lowering で受理する 3 サブステップに分割。
    - [x] **1.5-6 prep-access** — access modifier (`public` / `private` / `protected` / `readonly`) を no-op として受理。class field / method の modifier list 中で `Public` / `Private` / `Protected` / `Readonly` を意味解釈なしで通す(C 出力に可視性概念は無く、`readonly` も runtime 強制しない)、interface field の `readonly` も同様に no-op。`Static` / `Abstract` / `Override` は引き続き「class member modifier '<Kind>' is unsupported」で reject(意味が変わる構文は黙って通さない)。src/codegen.ts は `private` 137 箇所を多用するためここを通さないと食えない。回帰: `access_modifier.ts`(Counter `private`/`public` / Pair `readonly` / Mixed `protected`/`public readonly`/`private` / SmallBox implements Box の readonly interface field、合計 13 ケース)+ `access_modifier_static_fail.ts`(`static n: number;` reject)。
    - [x] **1.5-6 prep-void** — `void` 戻り値型を解禁。`TopazType` に `{ kind: "void" }` を追加、`cTypeName` は void を投げる(値型ヘルパ専用)、新規 `cReturnTypeName` は void を `"void"` に折り返してそれ以外は `cTypeName` に委譲。`typeFromAnnotation` は `VoidKeyword` を受理して `T_VOID` を返すが、`assertNotVoid(t, anchor, what)` ヘルパで非 return 位置(parameter type / variable type / class field / interface field / Array elem / Map K/V / Set elem / Iterator elem / union variant / fn-type param / arrow param)を明示 reject。return statement 検査: `return;` は currentReturnType が void のときのみ受理、`return <expr>;` は currentReturnType が void のとき reject。call-as-value: void を返す call expression を変数初期化 / 関数引数 / RHS で使うと `emitWithExpected` と `declareVar` の両 chokepoint で reject(statement position の bare call は引き続き OK = 副作用専用)。fn type / arrow 制約: `(p: T) => void` の fn type と arrow function の void return は両方 reject。method の void: class / interface method の return type は void OK、interface vtable wrapper は `sig.returnType.kind === "void"` のとき body を `<call>;` に分岐(`emitInterfaceVtableWrapper`)。src/codegen.ts は `void` 戻り 35 箇所を多用するためここを通さないと食えない。回帰: positive 1 本(`void_return.ts` = function void / 早期 bare return / class method void / `bumpBy(n)` / `reset()` / interface method void via vtable dispatch、合計 7 ケース)、fail 6 本(`void_return_bare_in_nonvoid_fail.ts` / `void_return_value_in_void_fail.ts` / `void_value_assign_fail.ts` / `void_param_fail.ts` / `void_array_fail.ts` / `void_fn_type_fail.ts`)。
    - [x] **1.5-6 prep-field-init** — field initializer (`x: T = init;`) の受理。class body 内で field 宣言と同時に初期化値を書くと、constructor body の冒頭(`__topaz_class_tag` 代入の直後・user statement の前)に declaration 順で `this.x = init;` が注入される。全 field が initializer 持ちで explicit ctor が無い class は zero-arg constructor が自動生成される。`ClassInfo.fieldInits: Map<string, ts.Expression>` を追加して `collectField` で initializer を保存、`emitConstructorDefinition` で field 列を順に walk して initializer を `emitWithExpected(init, fieldType)` 経由(string-literal widening / class→iface coercion が自動)で吐く。`verifyDefiniteFieldInit` は ctor body top-level の `this.f = ...` 集合に加えて `info.fieldInits.keys()` も definitely-initialized 扱い、partial init(一部 field のみ initializer、ctor 無し)は「class 'X' has fields but no constructor; add an explicit constructor or a field initializer for: <missing>」で reject。**critical fix: `containerMonomorphSlot` を struct def の前に再配置** — 旧順序は struct def → containerMonomorphSlot だったが、`class Pool { items: Array<Inner> = []; }` のように field 型に Array<class> を持つと `topaz_array_class_Inner` typedef が struct 内で未定義になり C compile が壊れる。新順序は (1) concrete class typedef fwd → (2) classMonoTypedefSlot → (3) iface typedef + vt struct fwd → (4) **containerMonomorphSlot**(新位置)→ (5) concrete class struct defs → (6) classMonoStructSlot → (7) iface vt struct defs。container マクロは class を pointer として参照するだけなので typedef fwd 経由で十分に満たせる。**JS-compat divergence**: declaration 順より後の field を init 内で参照すると calloc 初期値(number=0 / boolean=false / string=empty / reference=NULL)が見える(JS の field init semantics に揃え、TS の `--useDefineForClassFields` 相当)。src/codegen.ts は Emitter クラスで `private` field 35+ 個を全 default 初期化 + ctor 無しで宣言しているため、ここを通すために auto-ctor 生成が必須。回帰: positive 1 本(`field_initializer.ts` = (1) scalar literal init + 0-arg auto-ctor、(2) container init (Array / Map / Set) + 別 instance 独立、(3) explicit ctor との併用(field init が先、ctor body が後)、(4) explicit ctor で field init を上書き、(5) mixed (一部 initializer、一部 ctor body)、(6) declaration 順より後 field の forward-ref(calloc 値が見える)、(7) class instance を field init、(8) interface field に implementing class を init(class→iface coercion)、(9) `Array<class>` field init、(10) ctor body で field init 値を読んで上書き、(11) constructor 引数で field init を上書き、合計 25 ケース)、fail 2 本(`field_initializer_type_mismatch_fail.ts` = `count: number = "zero"` で型不一致 reject、`field_initializer_partial_fail.ts` = 一部 field のみ initializer + ctor 無しで partial init reject)。1.5-6 prep 全 3 ステップで合計 87 ケース全 pass、既存 75 ケースに regression 無し。
    - [x] **1.5-6 prep-type-alias** — `type X = T;` を thin substitution として受理。`Emitter.typeAliases: Map<string, { decl, resolved?, resolving }>` に登録、`typeFromAnnotation` の `TypeReferenceNode` branch で `typeParamScope` の直後・built-in `Array` 等の直前に lookup を挟み、初回参照時に RHS を再帰評価して結果を memoize、`resolving` フラグ ON 中の再 entry で `circular type alias 'X'` で reject。alias は erased — value 位置で参照すると identifier scope lookup で「unknown identifier 'X'」と落ちる(Scope に登録しないので自然に reject)、`implements` clause は引き続き interface 名直参照のまま。built-in / class / generic class / interface との名前衝突は declaration 時 reject、`type X<T> = ...` の generic alias は「generic type alias 'X' is unsupported」で reject(alias monomorph テーブル + substitution は object literal RHS とセットで再評価)、object literal type RHS (`type R = { a: number }`) は次サブステップなので現状 `typeFromAnnotation` の unsupported(TypeLiteral) で reject。非 root module にも alias を許可(許容 statement リストに type alias を追加)。回帰: positive 1 本(`type_alias.ts` = primitive / chain / Array / Map / Set / nested container / class / interface / Array<alias of class> / fn type / dunion / `T | undefined` / forward-ref 順序逆転 / generic class type arg / Iterator、合計 13 ブロック・21 出力)、fail 5 本(`type_alias_generic_fail` = generic alias reject、`type_alias_circular_fail` = `type A = B; type B = A;` 循環、`type_alias_object_literal_fail` = TypeLiteral 未対応、`type_alias_name_conflict_fail` = class と同名 alias、`type_alias_as_value_fail` = alias を expression 位置で参照)。合計 93 ケース全 pass。
  - [ ] **1.5-6 prep の残り** — object literal type(`{ a: T; b: U }`、src/ で 27 箇所、構造的 record か Map か anonymous class か方針未確定)。class struct と類似だが nominal vs structural の選択が要決定。
  - [ ] **1.5-6** — self-hosting 通過。`src/*.ts` を Topaz 自身でコンパイルできる状態にする。**parser 戦略をここで確定**(`src/codegen.ts` が `import * as ts from "typescript"` で TS の AST API そのものを使っているため、(a) parser を自前で書き直す、(b) `typescript` を opaque FFI として扱う、(c) parser だけ Node で走らせて AST を JSON で渡す、の三択を実装作業の中で選ぶ)。途中で踏んだ未対応機能は 1.5-N に折り返してフィードバックループを回す。
  - [ ] **1.5-X (オプション)** — `finally` 句および try body 内 return / break / continue 解禁。cleanup の lowering(`__cleanup__` attribute or 手書きの dispatch tree)とセット。self-hosting で実際に欲しくなった時点で着手、不要なら Phase 2 に持ち越し。

順序はあくまで現時点の見立てで、self-hosting に必要な機能から逆算して入れ替える。新機能を入れる時は **「コンパイラが自分自身をコンパイルできる範囲」がサブセットの下限**(`§3.3`)であることを忘れない。

### Phase 2: 実用性

- Promise / async-await(Fiber ベース実装)
- 基本 I/O(`std/fs`、`std/net`)
- regexp 統合
- bigint 統合(必要時のみリンク)
- ベンチマークスイート整備(`topaz_emit_number_shortest` を Ryu へ差し替え含む)

### Phase 3 以降: エコシステム

ここから先は「Effect-TS の延長線」的な発展:

- capability / effect 型追跡(`!{ fs.read, throw<E> }` 的シグネチャ)
- strict パッケージマニフェスト(npm 互換、`strict-ts.json` で宣言)
- LLM 駆動マイグレーションツール(既存 TS → strict 化)
- stdlib 整備
- Wasm バックエンド(WASI Preview 2/3)

---

## 7. 競合との立ち位置

| プロジェクト | アプローチ | このプロジェクトとの違い |
|---|---|---|
| Static Hermes | JS をフル受け入れ、sound 部分のみ最適化 | 我々は最初から非 sound を未対応にする |
| Porffor | JS フル仕様を目指す研究 | 我々は subset を最初から宣言、Test262 を追わない |
| AssemblyScript | TS 風の別言語、Wasm 専用 | 我々はネイティブもターゲット、構文も普通の TS により近く |
| Bun/Deno compile | ランタイム同梱 | 我々は真の AOT、バイナリは KB〜MB 級 |
| ReScript/PureScript | 別言語、別エコシステム | 我々は TS 構文・型を流用、エディタ体験そのまま |

---

## 8. 落とし穴(事前に意識しておく)

1. **「TS 構文」というラベルは諸刃**: 既存 TS 開発者が「ほとんど同じだから書ける」と思って入り、`any` 禁止で挫折する(AssemblyScript で実際起きた)。「これは TS ではない」と最初に強く伝えるブランディングが必要
2. **stdlib のサイズが寿命を決める**: Deno が成功して pkg が消えた理由はだいたいこれ
3. **「速い」を売りにすると失敗する**: ほとんどの TS 開発者は性能で困ってない。売りは「**バグの一クラス全体が消える**」「**小さい単機能バイナリが配れる**」
4. **structural typing の扱いを最初に決めないと後で苦しむ**: monomorphize 戦略を MVP から固める
5. **npm 互換性は意図的に切る**: 中途半端な互換性は混乱を生む。FFI 経由の明示的 binding のみ

---

## 9. 直近のアクション(未完了)

- [x] **1.5-2 着手前に self-hosting の最小到達点を割り出す**。完了。結果は `docs/archive/self-hosting-inventory.md`。要点:
  - 1.5-N の順序は (1) `node:*` / typescript パッケージ依存(parser 戦略)を **1.5-6 まで遅延**、(2) **1.5-3.5 新設**(`for-of` / arrow / template literal / destructuring / optional chaining / `??` / `!` / spread + `Array.map` 系 / `Map.values` を集中投入)、(3) **1.5-3 で discriminated union narrowing と `T | undefined` narrowing を明示**、(4) **1.5-5 (generic method / generic interface) は src/ で未使用のため self-hosting の前提ブロッカーではない**(1.5-6 の後ろに回せる)、で確定。
  - `src/` を Topaz で通すには、合計で `for-of` 58 / arrow 22 / template literal 280 / non-null `!` 18 / access modifier 84 / `as Extract<T, U>` narrowing 5 / discriminated union(`TopazType`)1 中核 / `T | undefined` 14+ ヘルパ戻り値、を片付ける必要がある。
  - **parser 戦略の決断は 1.5-6 に集約**(`import * as ts from "typescript"` を: (a) 自前 parser に置換、(b) opaque FFI、(c) parser だけ Node で走らせて AST を JSON 受け渡し、の三択を実装作業の中で選ぶ)。1.5-2 〜 1.5-5 のスコープには含めない。
- [x] **1.5-2 完了** (相対パス specifier 限定、循環検出、宣言の topo flatten、`export` 修飾子受理、非 root の executable は明示エラー)。決定ログは `docs/archive/implementation-log.md` 末尾。
- [ ] **generic class の未対応領域の棚卸し**(`class Box<T> implements I` / type parameter constraint / default type parameter / generic class を Map / Set の key にする方向)。self-hosting で踏むものが 1 つでもあれば 1.5-N のいずれかに組み込む、踏まないなら Phase 2 に降ろす。src/ では generic class / generic function はユーザー定義 0 件のため、self-hosting からの逆流は今のところ無い。
- [ ] generic 関数の戻り値が `Array<T>` の場合の monomorph 収集を、generic 関数経路と非 generic 経路で確実に同じ slot へ流すパスをドキュメント化(現状は self-hosting で踏むまで顕在化しない領域)。
- [x] **1.5-3 着手前の準備**: `inferType` を TypedAST 層に切り出す前段として、現状の codegen 内の型推論経路を別ファイル/別 module に物理分割するかは 1.5-3 のキックオフ時に判断 → **分けない方針で 1.5-3a に着手**。3300 行に収まっており、union/optional 表現(1.5-3b)と flow narrowing 機構(1.5-3d)を入れる段階でも `Emitter` クラスの中で完結する見込み。物理分割は self-hosting 通過(1.5-6)後に「型検査器が `src/typecheck.ts` の独立 module になっている」レイアウトへ寄せる方が、parser 戦略確定後の境界を考えやすい。
- [x] **1.5-3a 完了** (strict field init、`verifyDefiniteFieldInit` で constructor body の top-level 代入のみ集める保守実装)。決定ログは `docs/archive/implementation-log.md` 末尾。
- [x] **1.5-3b 完了** (TopazType に `undefined` / `union` variant、`T | undefined` 限定で型表現 + assignability + `=== undefined` lowering + property access の reject)。決定ログは `docs/archive/implementation-log.md` 末尾。
- [x] **1.5-3d 完了** (1.5-3c より先に narrowing を入れる方を選択)。`Scope` に narrowing overlay を追加、`if (x !== undefined) { ... }` の then narrowing と `if (x === undefined) return; /* rest */` の early-exit narrowing を実装、`alwaysExits` で「必ず抜ける」を保守的に判定。`examples/optional_narrow.ts` で 10 行回帰。決定ログは `docs/archive/implementation-log.md` 末尾。
- [x] **1.5-3c 完了**。scalar | undefined を `topaz_opt_<scalar>` の sentinel struct (`{ present: bool, value: T }`)で表現、class | undefined は `T *` (NULL = undefined)、interface | undefined は fat pointer (`.data == NULL` = undefined) を流用。`TOPAZ_MAP_DEFINE` を 6 引数化して `_get` が opt 戻りになるよう変更、value kind ごとに opt_t / wrap / absent を渡す方式に統一。`Map.get` の `inferType` が `V | undefined` を返すので、narrowing(1.5-3d)で `if (x !== undefined)` を書かないと使えない。`examples/map_set_basic.ts` と `examples/map_set_class_iface.ts` を narrowing パターンに書き換え、新規 `examples/optional_map_get.ts` で scalar / class / iface V の Map.get を 25 ケース pass。決定ログは `docs/archive/implementation-log.md` 末尾。次は 1.5-3e (discriminated union narrowing)。
- [x] **1.5-3e 完了**。class union (`Circle | Square`) を `kind: "literal"` 共有 discriminator で同定して fat pointer `{ topaz_string kind; void *data; }` に lower。`emitWithExpected` で class → dunion 構築(compound literal)、`emitSwitchStatement` で `switch (<id>.<discriminator>)` パターンを検出して per-case `scope.narrow()`、identifier emit で narrowed dunion → 元 class へ cast (`(topaz_class_<C> *)(<id>).data`)。`tryMakeDiscriminatedUnion` が union node の variants から全 class が同一 string-literal field を持つかチェック。`Scope.narrow` は 1.5-3d で入れた overlay にそのまま乗せた。`examples/dunion_basic.ts` (positive) と `examples/dunion_field_access_fail.ts` (`s.radius` を narrow 前に触ったらエラー) を追加、26 ケース全て pass。決定ログは `docs/archive/implementation-log.md` 末尾。次は 1.5-3f (catch `unknown` narrowing)。
- [x] **1.5-3f 完了**。TopazType に `{ kind: "unknown" }` を追加、`cTypeName` は `void *`。catch binding の型注釈を optional 化(`catch (e)` も `catch (e: unknown)` も `T_UNKNOWN`、`: ClassName` は引き続き受理)。`instanceof` を binary operator として追加、RTTI は全 class struct 先頭の `const char *__topaz_class_tag` フィールド(constructor で per-class static `topaz_class_<C>_tag` のアドレスを書く)を `*((const char * const *)x) == &topaz_class_<C>_tag` で参照する形に lower (NULL guard 付き)。`extractNarrowing` を拡張して `<id> instanceof <Class>` の positive 分岐で unknown → class に narrow、identifier emit が `void *` から concrete class pointer へ cast。多態検出は既存の `inferType` 型一致チェックで実質カバー済みで追加コード無し。`examples/catch_unknown.ts` (positive、`catch (e: unknown)` / `catch (e)` 両方 + nested rewrap + 自明な class instanceof) と `examples/catch_unknown_unnarrowed_fail.ts` (narrow せず field 触ったらエラー) を追加、28 ケース全て pass。これで **Phase 1.5-3 全体が完了**。次は 1.5-3.5 (syntactic sugar 集中投入: `for-of` / arrow + closure / template literal / destructuring / optional chaining / `??` / `!` / spread / `Array.map` 系 / `Map.values()`) か 1.5-4 (per-process arena)。決定ログは `docs/archive/implementation-log.md` 末尾。
- [x] **1.5-4 完了**。template literal 投入の前に arena を入れる方向を選択(template literal を `+` に lowering すると毎回 alloc が走るため、arena が無いと leak が peak memory に直結する)。`runtime/runtime.h` 先頭に chunk-based bump allocator (`topaz_arena_chunk` 32 bytes header + payload 16-byte aligned、初期 chunk 64KB、大きい alloc は専用 chunk) を embed、`topaz_arena_alloc` / `_calloc` / `_realloc` の 3 API を提供。runtime 内の malloc / calloc / realloc / free 呼び出しを全て arena 経由に置換(`topaz_string_concat`、Array `_new` & `_reserve`、Map `_new` & `_rehash`、Set `_new` & `_rehash`、Map / Set の `free(slots)` は単純削除)、codegen の class allocation (`topaz_class_<C>_new` の `calloc(1, sizeof(*self))`) を `topaz_arena_calloc` に置換、NULL guard も削除(arena は常に non-NULL 返却)。realloc は単純な alloc + memcpy(in-place 最適化は省略、Array doubling と Map rehash の grow しか出ないため amortized O(1)、peak は最大 +100%)。新規 `examples/arena_stress.ts` で Array 1000 push / class 1000 new / string 200 concat (~120KB、chunk 跨ぎ) / Map 500 set (rehash ~6 回) を 1 ファイルで総当たり、29 ケース全 pass。次は 1.5-3.5 (syntactic sugar 集中投入)。決定ログは `docs/archive/implementation-log.md` 末尾。
- [x] **1.5-3.5a 完了**。**template literal** を左結合 `topaz_string_concat` chain に lowering。`runtime/runtime.h` に `topaz_number_to_string` (既存 `topaz_emit_number_shortest` を arena 48-byte buf 書き出し版に refactor) と `topaz_boolean_to_string` (`"true"` / `"false"` の `static const char[]` 戻り) を追加、`topaz_console_log_number` も `topaz_number_to_string` 経由に統一して既存 `number_format` smoke が ToString の format 回帰として効くようにした。codegen 側で `ts.isTemplateExpression` を `emitExpression` / `inferType` の `isStringLiteral` 隣に追加、`emitStringLiteral` の signature を `TemplateHead` / `TemplateMiddle` / `TemplateTail` も受けるよう広げて escape ロジックを流用。substitution 型は number / boolean / string に限定(class / interface / array / map / set / unnarrowed `T | undefined` は明示エラー)、空フラグメントは skip するので `` `${name}` `` は concat 呼び出し 0 回まで畳む。`examples/template_literal.ts` で各 substitution 型 / 空フラグメント / narrow 後 / class field・method / `\t` escape / ループ内連続 concat の 20 行を網羅、`examples/template_literal_unsupported_fail.ts` で class instance 埋め込みエラーを fail 回帰、合計 30 success + fail ケース全 pass。**`double` は C キーワードなので example 内の関数名は `twice` に**(codegen 段の C 予約語衝突 sanitizer は 1.5-6 self-hosting で踏むまで保留)。次は arrow + closure と `for-of` を `Array.map` のために組で。決定ログは `docs/archive/implementation-log.md` 末尾。
- [x] **1.5-3.5b 完了**。**`for-of` over `Array<T>`** を index-based C for-loop へ lowering(`emitForOfStatement` を `emitForStatement` の直後に追加)。RHS の二重評価回避のため `topaz_array_<short> *__topaz_for_arr_N = <rhs>;` で snapshot、`for (size_t __topaz_for_idx_N = 0; __topaz_for_idx_N < __topaz_for_arr_N->len; __topaz_for_idx_N++)` で反復、body 先頭で `<elem> bindName = __topaz_for_arr_N->data[__topaz_for_idx_N];` を declare(`_at` ヘルパの bounds-check + NaN check は `i < len` の構造で不要、direct buffer 参照に切り替え)。binding は `const` / `let` の単一識別子のみ、destructuring・初期化子付き binding・複数宣言・`var`・`for await` は明示エラー、型注釈は elem と EXACT 一致を要求(class → interface coercion 等は意図的に通さない)。RHS が Map / Set / string の場合は hint 付きエラーで reject(`Map.values()` / Set iteration / string indexing は 1.5-3.5g で Iterator interface と一緒に)。`for-in` は構文丸ごと未対応(prototype 探索は JS 固有)。`recordArrayMonomorph(rhsType)` を for-of 経路でも呼び、`Array<Class>` / `Array<Iface>` monomorph が for-of だけで触れられた場合も typedef が emit される。`break` / `continue` / nested for-of は通常 for と同じ挙動(`checkContinueAllowed` が既に `ts.isForOfStatement` を受理)。新規 `examples/for_of_array.ts`(number / boolean / string / class(method 経由 mutation)/ interface(vtable dispatch)/ break / continue / nested / 空 / `let` rebind / array literal RHS の 17 行)、`examples/for_of_map_fail.ts`(Map RHS reject)、`examples/for_of_destructuring_fail.ts`(destructuring reject)、合計 33 ケース全 pass。**ループ変数の lifetime の宿題**: C ローカルは iteration ごとに再代入するだけ(per-iter 再 alloc しない)なので、1.5-3.5e で closure を入れた時に「ループ内 arrow が全部最後の値を見る」問題が出る。closure 着地時に再評価する。決定ログは `docs/archive/implementation-log.md` 末尾。
- [x] **1.5-3.5c 完了**。**`!` non-null assertion** と **`??` nullish coalescing** を `T | undefined` 専用の lowering として投入。`inferType` / `emitExpression` に `ts.isNonNullExpression(expr)` 分岐と `ts.SyntaxKind.QuestionQuestionToken` 分岐をそれぞれ追加。`!` は operand を tmp に snapshot して sentinel slot を check(scalar = `topaz_opt_T.present`、class = `T *` NULL、iface = fat pointer `.data == NULL`)、失敗時は `fputs("topaz: non-null assertion failed\n", stderr); abort();`(JS の `TypeError` とは divergence、stack trace 無し)。`??` は同じ sentinel 上の short-circuit ternary に lower。**チェーン**: `a ?? b ?? c` の中間結果を `T | undefined` のまま流すために、scalar の場合は RHS の型に応じて present branch を `__t` のまま yield(結果型 T | undefined)か `__t.value`(結果型 T)に切り替える分岐を入れた。class / iface は両ブランチが同じ pointer / fat pointer 表現なので分岐不要。RHS は `emitWithExpected` 経由なので class → iface coercion と string-literal → string widening が自動。**no-op は明示エラー**: 既に `T` 型の値に対する `!` / `??` は TS では warning のみだが、Topaz は assertion / fallback の意味が消えるため厳格に止める。新規 `examples/non_null_and_coalesce.ts`(scalar / class / iface の `Map.get` 経由 + 明示 `T | undefined` binding + class→iface coercion fallback + 二段チェーン + 関数中の `!` の 18 行)、`examples/non_null_non_optional_fail.ts` と `examples/coalesce_non_optional_fail.ts` で no-op reject、合計 36 ケース全 pass。`!!` (double bang)・`?.` (optional chaining)・`??=` (logical nullish assignment) は未対応のまま。決定ログは `docs/archive/implementation-log.md` 末尾。次は 1.5-3.5d (optional chaining `?.`)。
- [x] **1.5-3.5d 完了**。**optional chaining `?.`** の 3 形態(`a?.b` property access、`a?.b()` method call、`a?.[i]` element access)を `T | undefined` 受信者専用の lowering として投入。result 型は常に `U | undefined`(U は field / method-return / elem の inner 型)。`emitExpression` / `inferType` / `emitCall` の dispatch 分岐を増やし、本体は 7 private helper (`resolveOptionalReceiver` / `resolveOptionalFieldType` / `resolveOptionalMethodSig` / `resolveOptionalIndexType` / `lowerOptionalChain` / `emitOptionalPropertyAccess` / `emitOptionalElementAccess` / `emitOptionalMethodCall`)に分解。共通 lowering は `({ <recv-c-type> __topaz_oc_<N> = <recv>; (<is-absent>) ? <absent-literal> : <present>; })` の stmt-expression で receiver を tmp に 1 回 snapshot して sentinel slot を check、absent は `emitUndefinedLiteral`、present は `applyCoercion` で `U | undefined` に widening(scalar 経路の `topaz_opt_wrap_<T>` も自動)。class field は `(${tmp})->${f}`、iface field は vtable getter `(${tmp}).vt->get_${f}((${tmp}).data)`、class method は `topaz_class_<C>_method_<m>(${tmp}, args)`、iface method は `(${tmp}).vt->${m}((${tmp}).data, args)`、array index は既存 `topaz_array_<name>_at(${tmp}, idx)`(bounds-check / NaN guard 付き)。**no-op `?.` は明示エラー**(non-optional receiver には `?.` の short-circuit 意図が消える、`!` / `??` と同じ厳格化方針)。**`f?.()` optional call は明示 reject**(arrow + closure 1.5-3.5e で function-valued binding の C 表現が決まるまで保留)。**`a?.b.c` の mixin** は既存の union-rejection (`cannot access '.c' on union type`)で自然に reject、`a?.b!.c` のように明示 `!` を挟むパターンは通る。runtime.h は無修正。新規 `examples/optional_chain.ts`(positive、class / iface の field & method を `Map.get` 経由で hit / miss 両方、`Array<number> | undefined` への `?.[i]`、二段チェーン `links.get("h")?.next?.payload`、`?.` + `!` combined、`if (x !== undefined)` narrowing の合計 22 行)、`examples/optional_chain_non_optional_fail.ts`(non-optional reject)、`examples/optional_call_fail.ts`(`f?.()` reject)で error path を 2 本固定、合計 39 ケース全 pass、既存 36 ケースに regression 無し。決定ログは `docs/archive/implementation-log.md` 末尾。次は 1.5-3.5e (arrow + closure キャプチャ)。
- [x] **1.5-3.5e 完了**。**arrow function `(p: T) => R` + closure キャプチャ**を投入。`TopazType` に `{ kind: "fn", params, returnType }` variant を追加、C 表現は fat pointer struct `topaz_fn_a<N>__<param-tags>__to__<ret-tag> = { R (*fn)(void *env, ...); void *env; }`(arity prefix `a<N>` + per-type tag を `__` join した mangled name で per-(arity, signature) に唯一)。各 arrow は per-arrow 関数 `__topaz_arrow_<id>` と env struct `__topaz_arrow_env_<id>`(キャプチャがある場合のみ)を生成し、4 つの emit slot に分散配置: (a) `fnTypedefSlot`(union scan で全 fn 型 typedef を 1 度ずつ)、(b) `arrowFwdSlot`(env typedef + 関数 fwd decl、user fn fwd の前)、(c) `arrowDefSlot`(関数 def、user fn def の後・main の前)、(d) コンストラクト式自体は `(${fnTypeName}){ .fn = (...cast)&__topaz_arrow_<id>, .env = ${envInit} }` の compound literal。**capture 解析**は `Scope` の **barrier 機構**(`pushBarrier` で `stack.length` を floor として記録、通常 `lookup` は floor 未満を見ない / `lookupAcrossBarrier` だけ floor を跨いで外側 frame を覗ける)で実装。`collectCaptures` が arrow body を AST walk し、`isReferencePosition` で identifier の使われ方を読み(declaration site / property name / type ref / import specifier 等を除外)、local set に登録されておらず `lookupAcrossBarrier` で hit する identifier を「capture」として記録、nested arrow は inner 解析を再帰的に走らせて inner 自身が local 化していない自由変数を outer に promote。**barrier の push 順序が要**: `pushBarrier()` を先、`push()` を後の順で重ねないと「new frame index > floor」になって `lookup` が空範囲を走査して新規 binding を見失う(実装中に踏んだ critical bug、入れ替えで解決)。**by-value capture セマンティクス**(env struct alloc 時に scalar value を copy、class / iface は pointer / fat pointer を copy)を採用、これは JS の by-reference (let 変数の最新値を見る)と意図的に divergence するが、reference 型では pointer 共有なので実害は scalar に限られ、(a) C 表現の単純化、(b) per-iteration env alloc により for-of 内 arrow の loop var freshness が自動解決、の 2 つで pay off する。env init は `({ ${envName} *__e = topaz_arena_alloc(sizeof(${envName})); *__e = (${envName}){ ${...} }; __e; })` の stmt-expression で arena から取得。**call dispatch**: identifier callee が `functionSigs` にもなく `inferType(callee).kind === "fn"` ならば `emitFnValueCall` が `({ ${fnType} ${tmp} = ${callee}; ${tmp}.fn(${tmp}.env, ${args}); })` の stmt-expression に lower、tmp で fn 値を 1 回評価して env と fn pointer を取り出す。**contextual typing**: 独立式中の arrow (`(n) => n + 1;` のような表現)は param + return 注釈必須だが、`emitWithExpected(arrow, fnType)` 経路では expected の param 型を contextual に流すため、変数初期化 / 関数引数 / `return` 等のサイトで `(n) => n + 1` (注釈なし) が `(n: number) => number` の expected に合えば param 型を推論できる。**未対応**(明示エラー): (1) 関数値を Array / Map / Set に格納(1.5-3.5f で `Array.map` 実装と一緒に再評価、`elemTag` で fn を reject することで「no Array monomorph for element type topaz_fn_...」エラーになる)、(2) nested fn type を fn parameter / return 位置に書く(`(g: (n: number) => number) => number`、typedef slot が monomorph 順序より先に埋まる順序問題で 1.5-3.5f まで保留)、(3) generic arrow (`<T>(x: T) => x`)、(4) async arrow、(5) `f?.()` optional call、(6) function expression (`function() { ... }`)。新規 `examples/arrow_basic.ts`(18 行 positive、non-capturing / multi-param / boolean return / 0-arg / scalar capture の by-value divergence (`counter = 999` 後の `snapshot()` が 100 を返す)/ class reference capture / apply higher-order / arrow returning class / string capture を網羅)、`arrow_unannotated_fail.ts`(注釈無し独立式)、`arrow_in_container_fail.ts`(`Array<(n: number) => number>` 拒否)、`arrow_nested_fn_type_fail.ts`(`(g: (n: number) => number) => number` 拒否)で error path を 3 本固定、合計 43 ケース全 pass、既存 39 ケースに regression 無し。決定ログは `docs/archive/implementation-log.md` 末尾。次は **1.5-3.5f (`Array.map` / `.filter` / `.join` / `.includes` / `.slice`、Array storage に fn を入れる経路の解禁とセット)** → 1.5-3.5g (`Map.values()` + 最小 Iterator interface)。

---

## 参考リンク

- [matz/spinel](https://github.com/matz/spinel) — 直接のインスピレーション源
- [facebook/hermes](https://github.com/facebook/hermes)(`static_h` ブランチ) — Static Hermes
- [CanadaHonk/porffor](https://github.com/CanadaHonk/porffor) — JS/TS AOT 研究
- [AssemblyScript](https://www.assemblyscript.org/) — TS 構文の別言語の先例
- [Effect-TS](https://effect.website/) — TS 内で effect system を擬似実装した例
